name: Failing Pipeline

jobs:
  test:
    desc: "Test with failure"
    steps:
      - name: "Passing step"
        run: echo "This passes"
      - name: "Failing step"
        run: exit 1
      - name: "Never runs"
        run: echo "Should not run"
