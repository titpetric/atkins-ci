name: Matrix Test

vars:
  matrix: {}

jobs:
  matrix_test:
    matrix:
      goarch: ["amd64", "arm64"]
    steps:
      - name: "Print arch"
        run: |
          echo "Architecture: ${{ matrix.goarch }}"
